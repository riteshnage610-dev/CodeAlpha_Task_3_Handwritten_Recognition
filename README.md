# Task 3 — Handwritten Character Recognition
Identifies handwritten digits using image features and a neural-network classifier.

The project uses scikit-learn's built-in digits dataset so it runs offline. For the CodeAlpha extension, replace the loader with MNIST or EMNIST and reshape the images for a CNN.

Run: `pip install -r requirements.txt` then `python handwritten_character_recognition.py`.
